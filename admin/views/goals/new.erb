<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:goals, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:goals, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :goal, url(:goals, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'goals/form', :locals => { :f => f } %>
 <% end %>
</div>
