<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:teams, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:teams, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:teams, :edit, :id => @team.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :team, url(:teams, :update, :id => @team.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'teams/form', :locals => { :f => f } %>
  <% end %>
</div>
