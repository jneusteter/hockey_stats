<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:teams, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:teams, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :team, url(:teams, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'teams/form', :locals => { :f => f } %>
 <% end %>
</div>
