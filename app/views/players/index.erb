<% @players.each do |player| %>
  <%= link_to "#{player.first_name} #{player.last_name}", url(:players, :show, id: player.id), class: '', title: 'Player' %>
  <%= player.goals.count %>
  <hr>
<% end %>
